export function ManagementReservedPage() {
  return (
    <main style={{ minHeight: '100vh', display: 'grid', placeItems: 'center', padding: 32, fontFamily: 'Montserrat, sans-serif' }}>
      <section aria-labelledby="management-title" style={{ maxWidth: 720 }}>
        <h1 id="management-title" style={{ color: '#071C3A' }}>Área interna reservada</h1>
        <p>O sistema de gestão da Visa Fácil será desenvolvido futuramente sob <code>/app/*</code>.</p>
      </section>
    </main>
  );
}
