import { cmsHref, cmsImageSrc, cmsList, cmsTarget, cmsText, itemText, usePageSection } from '../content/SiteContentContext';

export function ExperienceSection({sectionId='experience'}:{sectionId?:string}) {
  const section=usePageSection(sectionId);const values=section?.values||{};const steps=cmsList(values.steps);const ctaTarget=cmsTarget(cmsText(values.ctaTarget,'_self'));const mainImage=cmsImageSrc(cmsText(values.mainImage));const miniImage=cmsImageSrc(cmsText(values.miniImage));
  return <section className="section spain" id="espanha"><div className="container spain__grid"><div className="spain__visual reveal"><div className="spain__photo">{mainImage&&<img src={mainImage} alt={cmsText(values.mainImageAlt,'Destino internacional')}/>}</div><div className="spain__stamp"><span>VF</span><b>VISA FÁCIL</b><small>Seu visto, sem complicação</small></div><div className="spain__mini-photo">{miniImage&&<img src={miniImage} alt={cmsText(values.miniImageAlt,'Viagem internacional')}/>}</div></div><div className="spain__content reveal reveal--delay"><span className="kicker kicker--light">{cmsText(values.kicker)}</span><h2>{cmsText(values.title)}</h2><p>{cmsText(values.description)}</p><div className="spain__steps">{steps.map((item,index)=><article key={index}><span>{itemText(item,'number',String(index+1).padStart(2,'0'))}</span><div><b>{itemText(item,'title')}</b><small>{itemText(item,'description')}</small></div></article>)}</div><a className="btn btn--coral btn--large" href={cmsHref(cmsText(values.ctaUrl),'#diagnostico')} target={ctaTarget} rel={ctaTarget==='_blank'?'noreferrer':undefined}>{cmsText(values.ctaLabel,'Analisar meu perfil')} <span>→</span></a><p className="legal-note">{cmsText(values.legalNote)}</p></div></div></section>
}
