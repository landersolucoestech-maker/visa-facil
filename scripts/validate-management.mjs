import { existsSync, readFileSync, readdirSync, statSync } from 'node:fs';
import { join, resolve } from 'node:path';
const root = process.cwd(); const read = (path) => readFileSync(resolve(root, path), 'utf8'); const failures = []; const assert = (condition, message) => { if (!condition) failures.push(message); };
function walkFiles(directory) { return readdirSync(directory).flatMap((entry) => { const fullPath = join(directory, entry); return statSync(fullPath).isDirectory() ? walkFiles(fullPath) : [fullPath]; }); }
const rootApplication = read('apps/web/src/RootApplication.tsx'); const main = read('apps/web/src/main.tsx'); const app = read('apps/web/src/modules/management/ManagementApp.tsx'); const shell = read('apps/web/src/modules/management/components/ManagementShell.tsx'); const dashboard = read('apps/web/src/modules/management/pages/ManagementDashboardPage.tsx');
const domainFiles = { clients:'apps/web/src/modules/clients/types/client.ts', processes:'apps/web/src/modules/processes/types/process.ts', documents:'apps/web/src/modules/documents/types/document.ts', interactions:'apps/web/src/modules/interactions/types/interaction.ts', chat:'apps/web/src/modules/chat/types/chat.ts', tasks:'apps/web/src/modules/tasks/types/task.ts', finance:'apps/web/src/modules/finance/types/finance.ts', notifications:'apps/web/src/modules/notifications/types/notification.ts' };
const pageFiles = { clients:'apps/web/src/modules/clients/pages/ClientsPage.tsx', clientDetail:'apps/web/src/modules/clients/pages/ClientDetailPage.tsx', processes:'apps/web/src/modules/processes/pages/ProcessesPage.tsx', processDetail:'apps/web/src/modules/processes/pages/ProcessDetailPage.tsx', documents:'apps/web/src/modules/documents/pages/DocumentsPage.tsx', interactions:'apps/web/src/modules/interactions/pages/InteractionsPage.tsx', chat:'apps/web/src/modules/chat/pages/ChatPage.tsx', tasks:'apps/web/src/modules/tasks/pages/TasksPage.tsx', calendar:'apps/web/src/modules/calendar/pages/CalendarPage.tsx', finance:'apps/web/src/modules/finance/pages/FinancePage.tsx', reports:'apps/web/src/modules/reports/pages/ReportsPage.tsx', settings:'apps/web/src/modules/settings/pages/SettingsPage.tsx' };
assert(rootApplication.includes("path === '/app'") && rootApplication.includes("path.startsWith('/app/')"), 'Management application must remain isolated under /app/*');
assert(rootApplication.includes('<ManagementApp />'), 'Root application must render ManagementApp for /app/*');
assert(main.includes("./modules/management/modal.css"), 'Shared modal styles must remain loaded');
assert(existsSync(resolve(root,'apps/web/src/modules/management/components/ManagementModal.tsx')), 'Shared ManagementModal component must remain present');
assert(!app.includes('localStorage') && !app.includes('sessionStorage'), 'Management foundation must not persist client data in browser storage');
assert(app.includes('history.pushState') && app.includes('import.meta.env.BASE_URL') && app.includes('toBrowserPath'), 'Management routing must preserve SPA navigation and deployment base');
assert(!app.includes('GlobalSearch') && !shell.includes('Buscar cliente, processo ou tarefa'), 'Management topbar search must remain removed');
assert(app.includes('NotificationCenter'), 'Frontend alert center must remain connected');
assert(app.includes('updateClient') && app.includes('updateProcess') && app.includes('updateDocument') && app.includes('updateInteraction') && app.includes('updateTask') && app.includes('updateFinancialEntry'), 'Session records must remain editable');
const requiredTopbarPages = ['Dashboard','Clientes','Cliente','Processos','Processo','Documentos','Atendimentos','VisaChat','Tarefas','Agenda','Financeiro','Relatórios','Configurações']; for (const title of requiredTopbarPages) assert(shell.includes(`title: '${title}'`), `Topbar metadata missing: ${title}`);
const routes = ['/app/clientes','/app/processos','/app/documentos','/app/atendimentos','/app/chat','/app/tarefas','/app/agenda','/app/financeiro','/app/relatorios','/app/configuracoes']; for (const route of routes) { assert(app.includes(`'${route}'`), `Route missing: ${route}`); assert(shell.includes(route)||dashboard.includes(route), `Route not exposed: ${route}`); }
for (const file of Object.values(domainFiles)) assert(existsSync(resolve(root,file)), `Missing domain file: ${file}`); for (const file of Object.values(pageFiles)) assert(existsSync(resolve(root,file)), `Missing page file: ${file}`);
const modalPages = [pageFiles.clients,pageFiles.processes,pageFiles.documents,pageFiles.interactions,pageFiles.tasks,pageFiles.finance]; for (const file of modalPages) { const source=read(file); assert(source.includes('ManagementModal'), `Create/view/edit must use modal on ${file}`); assert(source.includes('Visualização'), `View modal missing on ${file}`); assert(source.includes('Editar'), `Edit modal missing on ${file}`); }
assert(app.includes('Novo cliente') && app.includes('Novo processo') && app.includes('Adicionar documento') && app.includes('Registrar atendimento') && app.includes('Adicionar tarefa') && app.includes('Adicionar lançamento'), 'Topbar create actions must remain available');
assert(shell.includes('VisaFacilLogo') && shell.includes('seu visto, sem complicação'), 'Visa Fácil identity must remain');
assert(dashboard.includes('Processos por Status') && dashboard.includes('Financeiro (Resumo)') && dashboard.includes('Atendimentos Recentes'), 'Dashboard operational blocks must remain');
const processDomain=read(domainFiles.processes); assert(processDomain.includes("'diagnosis'")&&processDomain.includes("'completed'"),'Process stages incomplete'); const chatDomain=read(domainFiles.chat); assert(chatDomain.includes("'open'")&&chatDomain.includes("'closed'")&&chatDomain.includes('unreadCount'),'VisaChat lifecycle incomplete');
const validatedForms=['apps/web/src/modules/clients/components/ClientForm.tsx','apps/web/src/modules/clients/components/ClientEditForm.tsx','apps/web/src/modules/processes/components/ProcessForm.tsx','apps/web/src/modules/processes/components/ProcessEditForm.tsx','apps/web/src/modules/documents/components/DocumentChecklistForm.tsx','apps/web/src/modules/interactions/components/InteractionForm.tsx','apps/web/src/modules/tasks/pages/TasksPage.tsx','apps/web/src/modules/finance/pages/FinancePage.tsx']; for(const file of validatedForms) assert(read(file).includes('management-form-error'),`Visible form validation required: ${file}`);
const internalModules=resolve(root,'apps/web/src/modules'); for(const file of walkFiles(internalModules).filter((path)=>/\.(tsx?|css|jsx?)$/.test(path))) assert(!readFileSync(file,'utf8').toLowerCase().includes('kanban'),`Kanban is prohibited: ${file.replace(`${root}/`,'')}`);
assert(!app.includes('fetch('),'No backend endpoint should be simulated');
if(failures.length){console.error('Visa Fácil management contract validation failed:');failures.forEach((failure)=>console.error(`- ${failure}`));process.exit(1);} console.log('Visa Fácil management contract validation passed.');
