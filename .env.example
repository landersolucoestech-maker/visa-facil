# Dados demonstrativos locais.
# Só são carregados quando o Vite está em modo DEV e esta flag é explicitamente true.
# Builds de produção ignoram esta flag pela política definida em shared/runtimeFlags.ts.
VITE_CRM_MOCKS=false
